import AdminCalendarPage from "@/src/Admin/pages/AdminCalendarPage";

export const metadata = {
  title: "Calendar | NexCode Admin",
  description: "Schedule, deadlines, and reminders across projects",
};

export default function Page() {
  return <AdminCalendarPage />;
}
